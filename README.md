# Labubu Wholesale

Premium designer collectibles wholesale platform built with Next.js 14 and deployed on Cloudflare Pages.

## 🚀 Quick Start

```bash
# Install dependencies
npm install

# Run development server
npm run dev

# Build for production
npm run build

# Build for Cloudflare Pages
npm run pages:build
```

## 📁 Project Structure

```
labubu/
├── app/                    # Next.js App Router pages
│   ├── [locale]/          # Multi-language routes (es, fr, de, ja, ko)
│   ├── product/           # Product detail pages
│   ├── products/          # Product listing pages
│   └── ...
├── components/            # React components
├── data/                  # Product data and translations
├── lib/                   # Utility functions
├── locales/               # Language translations
├── public/                # Static assets (images, fonts)
└── ...
```

## 🌐 Multi-language Support

Supported languages:
- English (en) - Default
- Spanish (es)
- French (fr)
- German (de)
- Japanese (ja)
- Korean (ko)

## 📦 GitHub vs Cloudflare Pages

### GitHub Repository Should Include:
✅ Source code (`app/`, `components/`, `lib/`, `data/`, `locales/`)
✅ Configuration files (`next.config.mjs`, `package.json`, `tailwind.config.js`)
✅ Static assets (`public/`)
✅ Documentation (`README.md`)
✅ Lock file (`package-lock.json`)

### GitHub Repository Should NOT Include:
❌ `node_modules/` - Generated by npm install
❌ `.next/` - Build output, Cloudflare builds automatically
❌ `.env*` - Sensitive info, configure in Cloudflare dashboard
❌ `credentials.json`, `token.json` - API credentials

### Cloudflare Pages Deployment:
1. Connects to GitHub repository
2. Runs `npm install` (generates node_modules)
3. Runs `npm run pages:build` (generates build output)
4. Deploys static files to edge network

### Environment Variables:
Set in Cloudflare Pages dashboard, NOT in GitHub:
- `ROOT_URL` - Site URL
- Other API keys and secrets

## 🔍 SEO Features

- ✅ Static Site Generation (SSG) for all pages
- ✅ Multi-language hreflang tags
- ✅ JSON-LD structured data (Organization, Product, BreadcrumbList)
- ✅ XML Sitemap with hreflang
- ✅ Canonical URLs
- ✅ Open Graph & Twitter Cards
- ✅ robots.txt configuration

## 🛠️ Tech Stack

- **Framework**: Next.js 14 (App Router)
- **Styling**: Tailwind CSS
- **UI Components**: Radix UI
- **Deployment**: Cloudflare Pages
- **Runtime**: Edge Runtime

## 📝 Development Notes

### Adding New Products
Edit `data/product.js` to add new products.

### Adding Translations
1. Add translations to `locales/{lang}.js`
2. Update `data/auto-translate.js` for product translations

### Building for Cloudflare
```bash
npm run pages:build
```

## 📄 License

Private - All rights reserved.
