# Labubu Project

多主题网站项目，支持主题/模板切换。

## 📁 项目结构

```
labubu/
├── data/                      # 📦 共用数据 (所有主题共享)
│   ├── product.js            # 产品数据
│   ├── products.js           # 产品分类
│   ├── home.js               # 首页文案
│   ├── about.js              # 关于页面文案
│   ├── contact.js            # 联系页面文案
│   ├── privacy.js            # 隐私政策
│   ├── terms.js              # 服务条款
│   └── auto-translate.js     # 自动翻译
│
├── locales/                   # 🌐 多语言翻译 (所有主题共享)
│   ├── en.js
│   ├── es.js
│   ├── fr.js
│   ├── de.js
│   ├── ja.js
│   └── ko.js
│
├── public/                    # 🖼️ 共用静态资源
│   └── product/              # 产品图片
│
├── themes/                    # 🎨 主题目录
│   ├── theme.config.js       # 主题配置
│   └── labubu/               # Labubu 主题
│       ├── app/              # Next.js 页面布局
│       ├── components/       # UI 组件
│       ├── public/           # 主题专属图片 (logo, 装饰图等)
│       ├── lib/              # 工具函数
│       ├── data -> ../../data        # 符号链接到共用数据
│       ├── locales -> ../../locales  # 符号链接到共用翻译
│       ├── tailwind.config.js
│       └── package.json
│
├── package.json              # 项目根配置
└── README.md
```

## 🔄 共用 vs 主题专属

| 类型 | 位置 | 说明 |
|------|------|------|
| **共用数据** | `/data/` | 产品、文案、翻译 - 所有主题共享 |
| **产品图片** | `/public/product/` | 产品封面图、详情图 - 共享 |
| **共用翻译** | `/locales/` | 多语言文件 |
| **主题布局** | `/themes/xxx/app/` | 页面结构、路由 |
| **主题组件** | `/themes/xxx/components/` | UI 组件 |
| **主题样式** | `/themes/xxx/tailwind.config.js` | 样式配置 |
| **主题图片** | `/themes/xxx/public/` | logo、首页装饰图、页面背景图 |

### 📸 图片存放规则

```
labubu/
├── public/product/           # 🛒 产品图片 (共用)
│   └── xxx.png              # 产品封面、详情图
│
└── themes/labubu/public/     # 🎨 主题图片 (专属)
    ├── logo1.png            # 网站 Logo
    ├── home/                # 首页布局图片
    │   ├── Cover-image.png  # Hero 背景
    │   ├── 1.png, 2.png     # 装饰图
    │   └── ...
    ├── about/               # 关于页图片
    └── contact/             # 联系页图片
```

**规则**: 
- 产品相关图片 → 根目录 `/public/product/`
- 页面布局装饰图 → 主题目录 `/themes/xxx/public/`

## 🚀 快速开始

```bash
# 1. 安装主题依赖
npm run theme:install

# 2. 开发模式
npm run dev

# 3. 构建
npm run build
```

## 🎨 主题系统

### 切换主题
编辑 `themes/theme.config.js`:
```javascript
module.exports = {
  activeTheme: 'labubu',  // 修改为其他主题名
};
```

### 创建新主题
1. 复制 `themes/labubu/` 为新文件夹
2. 修改布局、组件、样式
3. 创建符号链接到共用数据:
   ```bash
   cd themes/new-theme
   ln -s ../../data data
   ln -s ../../locales locales
   ```
4. 在 `theme.config.js` 中注册

## 🚢 部署到 Vercel

### 1. 连接 GitHub 仓库
在 Vercel Dashboard 中导入项目

### 2. 配置项目设置
- **Root Directory**: `themes/labubu`
- **Framework Preset**: Next.js
- **Build Command**: `npm run build`
- **Output Directory**: `.next`

### 3. 配置环境变量
在 Vercel 项目设置 → Environment Variables 中添加:

| 变量名 | 说明 |
|--------|------|
| `RESEND_API_KEY` | Resend API 密钥 ([获取](https://resend.com/api-keys)) |
| `CONTACT_EMAIL` | 接收表单提交的邮箱 |
| `FROM_EMAIL` | 发件人邮箱 (需在 Resend 验证域名) |

### 4. 配置 Resend
1. 注册 [Resend](https://resend.com) 账号
2. 添加并验证你的域名
3. 创建 API Key
4. 将 API Key 添加到 Vercel 环境变量

## 📧 邮件功能

项目集成了 Resend 邮件服务，支持:
- **联系表单**: 客户询价/留言 → 发送到管理员邮箱
- **订阅表单**: 用户订阅 → 通知管理员 + 发送欢迎邮件

### 本地测试
1. 复制 `.env.example` 为 `.env.local`
2. 填入你的 Resend API Key
3. 测试时可使用 `onboarding@resend.dev` 作为发件人

## 📝 修改内容

- **修改产品数据**: 编辑 `/data/product.js`
- **修改页面文案**: 编辑 `/data/home.js`, `/data/about.js` 等
- **修改翻译**: 编辑 `/locales/` 下的文件
- **修改布局样式**: 编辑 `/themes/labubu/` 下的文件
